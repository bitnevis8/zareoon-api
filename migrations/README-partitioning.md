-- =============================================================================
-- پارتیشن‌بندی MySQL — عمداً روی جداول زنده اعمال نشده
-- =============================================================================
-- چرا الان نه؟
-- 1) RANGE PARTITION روی messages/files نیاز دارد PRIMARY KEY شامل ستون پارتیشن
--    باشد (مثلاً (id, created_at)). این تغییر FKها و Sequelize را می‌شکند.
-- 2) برای میلیون‌ها رکورد، ایندکس ترکیبی + cursor pagination معمولاً ROI بالاتری
--    از پارتیشن دارد و بدون downtime سنگین است.
--
-- چه زمانی پارتیشن مفید است؟
-- - messages / files / transaction_history وقتی هر کدام > ~20–50M ردیف دارند
-- - نیاز به DROP سریع دادهٔ قدیمی (مثلاً ARCHIVE سال قبل)
--
-- الگوی پیشنهادی آینده (آرشیو، نه پارتیشن اجباری):
--   CREATE TABLE messages_archive LIKE messages;
--   INSERT INTO messages_archive SELECT * FROM messages WHERE created_at < '2025-01-01';
--   DELETE FROM messages WHERE created_at < '2025-01-01' LIMIT 10000; -- دسته‌ای
--
-- اگر بعداً پارتیشن لازم شد، روی جدول جدید بدون FK شروع کنید و با dual-write
-- مهاجرت دهید — هرگز ALTER PARTITION روی جدول production با FK نزنید.
-- =============================================================================
