# زیبال (منبع اصلی: api/config/*.json — این‌ها اختیاری برای override هستند)
ZIBAL_MERCHANT=
# true = درگاه آزمایشی (merchant=zibal) | false = واقعی
ZIBAL_SANDBOX=true
# آدرس بازگشت بعد از پرداخت
ZIBAL_CALLBACK_URL=http://localhost:3001/pricing/callback
FRONTEND_URL=http://localhost:3001
